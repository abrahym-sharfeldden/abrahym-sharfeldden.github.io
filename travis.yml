language: node_js
node_js:
  - 17.4.0
cache: npm
directories:
  - node_modules
install:
  - npm install
script:
  - npm test
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep_history: true
  local_dir: build
  on:
    branch: master
